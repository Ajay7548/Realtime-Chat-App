config:
  target: "wss://realtime-chat-app-production-11df.up.railway.app"
  phases:
    - duration: 5
      arrivalRate: 1   # 2 per second = ~60 users total
scenarios:
  - engine: "ws"
    flow:
      - send: "hello"
      - think: 2
      - send: "test message"
